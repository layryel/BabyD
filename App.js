import React from "react";
import "react-native-gesture-handler";
import { Routes } from "./src/Routes"; // Certifique-se de que o caminho esteja correto

const App = () => {
  return <Routes />; // Apenas renderiza as rotas
};

export default App;
